# Scripts — Claude Code only

These Python scripts run in Claude Code (Bash available) but NOT in Cowork.

In Cowork, the equivalent logic is embedded directly in commands and agents
as inline formulas. See:
- `commands/sentinel.md` → triage formula (Step 1)
- `agents/noise-calculator.md` → divergence / CV / IQR algorithms
- `agents/calibration-coach.md` → Brier score calculation
